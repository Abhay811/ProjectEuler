#include "temp.hpp"

void solve(vector<vector<int>> &arr, int i, int j)
{
  if ()
}
int main()
{
  vector<vector<int>> arr;
  for (int i = 1; i <= 15; ++i)
  {
    vector<int> a(i);
    for (auto &it : a)
      cin >> it;
    arr.emplace_back(a);
  }
  solve(arr);
}